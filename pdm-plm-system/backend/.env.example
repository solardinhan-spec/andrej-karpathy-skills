# .env.example - 로컬 개발 환경 설정 참고 (실제 값은 .env.local에 작성)
# ※ 이 디렉토리(backend/)가 firebase.json의 functions.source 입니다.
NODE_ENV=development

# GCP 프로젝트
PROJECT_ID=pdm-plm-semiconductor
STORAGE_BUCKET=pdm-drawings-semiconductor
FIRESTORE_DATABASE=(default)
REGION=asia-northeast3

# 로컬 개발 시 서비스 계정 키 경로 (.gitignore에 포함된 파일)
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Autodesk APS(Forge) — DWG 변환/뷰어 (운영은 Secret Manager 사용)
APS_CLIENT_ID=YOUR_APS_CLIENT_ID
APS_CLIENT_SECRET=YOUR_APS_CLIENT_SECRET
APS_BUCKET_KEY=pdm-dwg-derivatives

# Google Drive 백업 폴더 ID (Drive URL에서 마지막 폴더 ID)
DRIVE_FOLDER_ID=YOUR_GOOGLE_DRIVE_FOLDER_ID
