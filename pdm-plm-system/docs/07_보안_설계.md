# 07. 보안 설계

## 7.1 역할 기반 접근 제어 (RBAC)

| 역할 | 설명 | 허용 작업 |
|------|------|-----------|
| `admin` | 시스템 관리자 | 모든 작업 + **계정 1회 발급** + 사용자 관리 |
| `reviewer` | 설계 검토자 / 팀장 | 조회 + 승인. **검토자 2명 전원 승인 시 Released** |
| `designer` | 설계 엔지니어 | 조회 + 품목/도면 등록 + 제출 + BOM 편집 |
| `viewer` | 협력사 / 제조팀 | Released 도면 조회만 가능 |

> **계정 정책:** 자가 가입 없음. `admin`만 `POST /api/admin/users`로 계정을 1회 발급한다.
> 클라이언트에서 Firebase `createUser`를 호출하지 않으며, `users/{uid}` 문서가
> 없는 계정은 인증 미들웨어에서 접근이 거부된다.

> **승인 정책:** 상태 전환(In Review → Released)은 보안 규칙에서 직접 허용하지 않고
> **백엔드 트랜잭션**(`/api/drawings/:dwgNo/approve`)에서만 수행한다. 클라이언트의
> 직접 status 쓰기는 차단하여 "검토자 2명 전원" 규칙을 서버에서 강제한다.

## 7.2 Firestore Security Rules

```javascript
// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ─────────────────────────────────────────────────────────
    // 역할 헬퍼 함수
    // ─────────────────────────────────────────────────────────

    function isAuthenticated() {
      return request.auth != null;
    }

    function getUserRole() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role;
    }

    function isAdmin() {
      return isAuthenticated() && getUserRole() == 'admin';
    }

    function isReviewer() {
      return isAuthenticated() && getUserRole() in ['admin', 'reviewer'];
    }

    function isDesigner() {
      return isAuthenticated() && getUserRole() in ['admin', 'reviewer', 'designer'];
    }

    function isAnyUser() {
      return isAuthenticated() && getUserRole() in ['admin', 'reviewer', 'designer', 'viewer'];
    }

    // ─────────────────────────────────────────────────────────
    // 품목 마스터 (items) - itemCode 유일
    // ─────────────────────────────────────────────────────────
    match /items/{itemCode} {
      allow read: if isAnyUser();
      allow create, update: if isDesigner();
      allow delete: if false; // 품목 삭제 불가
    }

    // ─────────────────────────────────────────────────────────
    // 도면 (drawings) - dwgNo 유일키 + 리비전 서브컬렉션 (불변)
    // ※ 상태 전환은 백엔드 트랜잭션에서만 수행 (클라 직접 쓰기 차단)
    // ─────────────────────────────────────────────────────────
    match /drawings/{dwgNo} {
      allow read: if isAnyUser()
        && (getUserRole() != 'viewer' || resource.data.status == 'Released');
      // 클라이언트 직접 쓰기 전면 차단 — 업로드/제출/승인 모두 백엔드 API 경유
      allow write: if false;

      // 리비전 스냅샷도 동일하게 읽기만, 쓰기는 백엔드 전용
      match /revisions/{rev} {
        allow read: if isAnyUser()
          && (getUserRole() != 'viewer' || resource.data.status == 'Released');
        allow write: if false;
      }
    }

    // ─────────────────────────────────────────────────────────
    // BOM 구조 (bom) - itemCode 단위
    // ─────────────────────────────────────────────────────────
    match /bom/{bomId} {
      allow read: if isAnyUser();
      allow write: if isDesigner();
      allow delete: if isAdmin();
    }

    // ─────────────────────────────────────────────────────────
    // 채번 시퀀스 (sequences) - 백엔드 서비스 계정만 접근
    // ─────────────────────────────────────────────────────────
    match /sequences/{seqId} {
      allow read, write: if false; // 클라이언트 직접 접근 완전 차단
    }

    // ─────────────────────────────────────────────────────────
    // 사용자 프로필 (users) - 관리자 1회 발급
    // ─────────────────────────────────────────────────────────
    match /users/{userId} {
      // 본인 또는 관리자만 조회
      allow read: if isAuthenticated() && (request.auth.uid == userId || isAdmin());
      // 생성/역할 변경은 백엔드 Admin API(서비스 계정)로만 — 클라 직접 쓰기 차단
      allow write: if false;
    }

    // ─────────────────────────────────────────────────────────
    // 감사 로그 (audit_logs) - 읽기 전용
    // ─────────────────────────────────────────────────────────
    match /audit_logs/{logId} {
      allow read: if isAdmin();
      allow write: if false; // 백엔드 서비스 계정만 쓰기
    }
  }
}
```

## 7.3 Cloud Storage 접근 규칙

```json
// storage.rules
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // DWG 도면 파일 - 인증된 사용자만 접근
    match /drawings/{dwgNo}/{revFolder}/{fileName} {
      // 모든 인증 사용자 읽기 가능 (Signed URL로 단기 접근 제어)
      allow read: if request.auth != null;
      // 업로드는 백엔드 서비스 계정만 (서버 측에서만 처리)
      allow write: if false;
    }

    // 백업 파일
    match /backups/{allPaths=**} {
      allow read: if request.auth.token.role == 'admin';
      allow write: if false;
    }
  }
}
```

## 7.4 Secret Manager 활용

```bash
# 서비스 계정 키를 Secret Manager에 안전하게 보관
gcloud secrets create pdm-sa-key --replication-policy=automatic
gcloud secrets versions add pdm-sa-key --data-file=serviceAccountKey.json

# Drive OAuth2 토큰 저장
gcloud secrets create google-drive-oauth-token --replication-policy=automatic
echo -n "YOUR_OAUTH_TOKEN" | gcloud secrets versions add google-drive-oauth-token --data-file=-

# Cloud Functions(서비스 계정)에서 Secret 접근 권한 부여
gcloud secrets add-iam-policy-binding pdm-sa-key \
  --member="serviceAccount:pdm-backend-sa@pdm-plm-semiconductor.iam.gserviceaccount.com" \
  --role="roles/secretmanager.secretAccessor"
```

```javascript
// Secret Manager에서 런타임 시 키 로드
const { SecretManagerServiceClient } = require('@google-cloud/secret-manager');

async function getSecret(secretName) {
  const client = new SecretManagerServiceClient();
  const [version] = await client.accessSecretVersion({
    name: `projects/${process.env.PROJECT_ID}/secrets/${secretName}/versions/latest`
  });
  return version.payload.data.toString('utf8');
}
```

## 7.5 감사 로그 (Audit Log) 미들웨어

```javascript
// src/middleware/auditLog.js
const admin = require('firebase-admin');

function auditLog(action) {
  return async (req, res, next) => {
    const originalJson = res.json.bind(res);

    res.json = async (body) => {
      if (body.success) {
        await admin.firestore().collection('audit_logs').add({
          action,
          userId: req.user?.uid,
          userName: req.user?.name || req.user?.email,
          role: req.user?.role,
          params: { ...req.params, ...req.body },
          ip: req.ip,
          userAgent: req.headers['user-agent'],
          result: 'success',
          timestamp: admin.firestore.FieldValue.serverTimestamp()
        }).catch(err => console.error('[AuditLog] 저장 실패:', err.message));
      }
      return originalJson(body);
    };

    next();
  };
}

module.exports = { auditLog };
```

```javascript
// 라우터에서 사용 예시
router.post('/approve', verifyToken, auditLog('DRAWING_APPROVE'), async (req, res) => { ... });
router.post('/upload', verifyToken, auditLog('DRAWING_UPLOAD'), upload.fields(...), async (req, res) => { ... });
```

## 7.6 보안 체크리스트

```
✅ 인증/인가
  [ ] Firebase Authentication 활성화
  [ ] 모든 API 엔드포인트에 verifyToken 미들웨어 적용
  [ ] 역할별 권한 분리 (RBAC) 구현
  [ ] Firestore Security Rules 배포

✅ 데이터 보호
  [ ] Cloud Storage uniform bucket-level access 설정
  [ ] public access prevention 활성화
  [ ] Storage 버전 관리(versioning) 활성화
  [ ] 도면 삭제 규칙 비활성화 (Immutable)

✅ 네트워크 보안
  [ ] HTTPS 강제 적용 (Firebase Hosting + Cloud Functions 기본)
  [ ] CORS 운영 도메인 화이트리스트만 허용
  [ ] Cloud Functions maxInstances로 폭주 제어
  [ ] APS client id/secret을 Secret Manager로 관리 (코드 하드코딩 금지)

✅ 시크릿 관리
  [ ] .env, serviceAccountKey.json .gitignore 추가
  [ ] Secret Manager로 민감 정보 이관
  [ ] 서비스 계정 최소 권한 원칙(Least Privilege)

✅ 감사
  [ ] 모든 상태 변경 작업에 Audit Log 기록
  [ ] Cloud Audit Logs 활성화 (GCP 수준)
  [ ] 로그 보존 기간 설정 (최소 2년)
```
